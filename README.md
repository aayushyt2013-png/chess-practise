*to activate, enter in terminal*
cd /workspaces/chess-practise && export STOCKFISH_PATH=/usr/games/stockfish && python -m py_compile app.py && python app.py
*before that*
 sudo apt updatete
sudo apt install -y stockfish 
WITH 
pip install -r requirements.txt
